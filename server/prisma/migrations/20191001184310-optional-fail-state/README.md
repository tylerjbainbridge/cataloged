# Migration `20191001184310-optional-fail-state`

This migration has been generated by Tyler Bainbridge at 10/1/2019, 6:43:10 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."File" DROP COLUMN "isFailed",
ADD COLUMN "isFailed" boolean   ,
DROP COLUMN "isUploaded",
ADD COLUMN "isUploaded" boolean   ;

ALTER TABLE "public"."UploadGroup" DROP COLUMN "isComplete",
ADD COLUMN "isComplete" boolean   ,
DROP COLUMN "isFailed",
ADD COLUMN "isFailed" boolean   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191001181611-init..20191001184310-optional-fail-state
--- datamodel.dml
+++ datamodel.dml
@@ -82,10 +82,10 @@
   width  Int?
   height Int?
-  isUploaded Boolean
-  isFailed   Boolean
+  isUploaded Boolean?
+  isFailed   Boolean?
   item        Item
   user        User
   uploadGroup UploadGroup
@@ -95,10 +95,10 @@
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  isComplete Boolean
-  isFailed   Boolean
+  isComplete Boolean?
+  isFailed   Boolean?
   user  User
   files File[]
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191001184310-optional-fail-state)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191001184310-optional-fail-state'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
