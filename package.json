{
  "scripts": {
    "start": "npm-run-all --parallel start:*",
    "start:app": "cd web && yarn run start",
    "build:app": "cd web && yarn run build:app",
    "prebuild:app": "yarn run install:web",
    "build:landing-page": "cd landing-page && yarn run build",
    "prebuild:landing-page": "yarn run install:landing-page",
    "start:server": "cd server && yarn run start",
    "build:server": "cd server && yarn run build",
    "prebuild:server": "yarn run install:server",
    "dev": "npm-run-all --parallel dev:*",
    "dev:server": "pushd server && yarn run dev && popd",
    "dev:app": "pushd web && yarn run start && popd",
    "install:server": "cd server && yarn install",
    "install:landing-page": "cd landing-page && yarn install",
    "install:app": "cd web && yarn install",
    "format": "npx prettier '*/**/*.{ts,tsx}' --write"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "typescript": "^3.7.5"
  }
}
