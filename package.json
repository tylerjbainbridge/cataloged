{
  "scripts": {
    "start": "npm-run-all --parallel start:*",
    "start:web": "cd app/web && yarn run start",
    "build:web": "cd web && yarn install && yarn run build:app",
    "build:landing-page": "cd landing-page && yarn install && yarn run build",
    "start:server": "cd server && yarn run start",
    "build:server": "cd server && yarn install && yarn run build",
    "prebuild:server": "yarn run install:server",
    "dev": "npm-run-all --parallel dev:*",
    "dev:server": "pushd server && yarn run dev && popd",
    "dev:web": "pushd app/web && yarn run dev && popd",
    "install:server": "cd server && yarn install",
    "install:landing-page": "cd landing-page && yarn install",
    "format": "npx prettier '*/**/*.{ts,tsx}' --write"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "npm-run-all": "^4.1.5",
    "react": "^16.13.1"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "typescript": "^3.7.5"
  }
}
